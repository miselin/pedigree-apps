addons:
  apt:
    packages:
    - fakechroot
cache:
  directories:
  - standalone
install: pip install -r requirements.txt
language: python
matrix:
  include:
  - compiler: gcc
    env: TARGET=amd64 PACKAGE=libtool
    os: linux
    python: '2.7'
    sudo: true
notifications:
  email: false
script: ./travis.sh
sudo: false
