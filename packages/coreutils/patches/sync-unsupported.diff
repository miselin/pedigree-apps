diff --git a/src/system.h b/src/system.h
--- a/src/system.h
+++ b/src/system.h
@@ -471,7 +471,8 @@
 #endif
 
 #if ! HAVE_SYNC
-# define sync() /* empty */
+# define sync() \
+  error (EXIT_FAILURE, ENOSYS, _("global synchronization is not supported"))
 #endif
 
 /* Compute the greatest common divisor of U and V using Euclid's
