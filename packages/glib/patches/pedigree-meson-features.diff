--- a/meson.options
+++ b/meson.options
@@ -154,6 +154,6 @@
 
 option('file_monitor_backend',
        type : 'combo',
-       choices : ['auto', 'inotify', 'kqueue', 'libinotify-kqueue', 'win32'],
+       choices : ['auto', 'none', 'inotify', 'kqueue', 'libinotify-kqueue', 'win32'],
        value : 'auto',
        description : 'The name of the system API to use as a GFileMonitor backend')
--- a/meson.build
+++ b/meson.build
@@ -500,7 +500,8 @@
     return statx (AT_FDCWD, "/", AT_SYMLINK_NOFOLLOW, STATX_BASIC_STATS | STATX_BTIME, &stat_buf);
   }
   '''
-if host_system != 'android' and cc.compiles(statx_code, name : 'statx() test')
+if host_system not in ['android', 'pedigree'] and \
+   cc.compiles(statx_code, name : 'statx() test')
   glib_conf.set('HAVE_STATX', 1)
 endif
 
@@ -832,10 +833,21 @@
 # SVR4 sysinfo() does not behave the same as sysinfo() on other Unixes
 if host_system != 'sunos'
   functions += ['sysinfo']
+endif
+
+# Pedigree's musl headers expose compatibility wrappers for these Linux APIs,
+# but the kernel does not implement them yet. Keep GLib on its portable paths.
+pedigree_unavailable_functions = []
+if host_system == 'pedigree'
+  pedigree_unavailable_functions = [
+    'close_range', 'copy_file_range', 'epoll_create1', 'fallocate',
+    'inotify_init1', 'lchmod', 'lchown', 'prlimit', 'recvmmsg', 'sendmmsg',
+    'splice', 'sysinfo', 'unshare', 'utimensat'
+  ]
 endif
 
 foreach f : functions
-  if cc.has_function(f)
+  if f not in pedigree_unavailable_functions and cc.has_function(f)
     define = 'HAVE_' + f.underscorify().to_upper()
     glib_conf.set(define, 1)
     glib_conf_prefix = glib_conf_prefix + '#define @0@ 1\n'.format(define)
@@ -970,7 +982,8 @@
   glib_conf.set('HAVE_LOFF_T', 1)
 endif
 
-if cc.has_function('unshare', prefix: '#include <sched.h>', args: '-D_GNU_SOURCE')
+if 'unshare' not in pedigree_unavailable_functions and \
+   cc.has_function('unshare', prefix: '#include <sched.h>', args: '-D_GNU_SOURCE')
   glib_conf.set('HAVE_UNSHARE', 1)
 endif
 
@@ -1054,7 +1067,7 @@
                }''', name : 'futex(2) system call')
   glib_conf.set('HAVE_FUTEX', 1)
 endif
-if cc.compiles('''#include <linux/futex.h>
+if host_system != 'pedigree' and cc.compiles('''#include <linux/futex.h>
                #include <sys/syscall.h>
                #include <unistd.h>
                int main (int argc, char ** argv) {
@@ -1065,7 +1078,7 @@
 endif
 
 # Check for eventfd(2)
-if cc.links('''#include <sys/eventfd.h>
+if host_system != 'pedigree' and cc.links('''#include <sys/eventfd.h>
                #include <unistd.h>
                int main (int argc, char ** argv) {
                  eventfd (0, EFD_CLOEXEC);
@@ -1075,7 +1088,7 @@
 endif
 
 # Check for ppoll(2)
-if cc.links('''#define _GNU_SOURCE
+if host_system != 'pedigree' and cc.links('''#define _GNU_SOURCE
                #include <poll.h>
                #include <stddef.h>
                int main (int argc, char ** argv) {
@@ -1088,7 +1101,7 @@
 endif
 
 # Check for pidfd_open(2)
-if cc.links('''#include <sys/syscall.h>
+if host_system != 'pedigree' and cc.links('''#include <sys/syscall.h>
                #include <sys/wait.h>
                #include <linux/wait.h>
                #include <unistd.h>
--- a/glib/glib-unix.c
+++ b/glib/glib-unix.c
@@ -996,6 +996,10 @@
     }
 
   return g_strdup (file_path);
+#elif defined (__pedigree__)
+  g_set_error (error, G_FILE_ERROR, G_FILE_ERROR_NOSYS,
+               "g_unix_fd_query_path() not supported on Pedigree");
+  return NULL;
 #elif defined (__GNU__)
   /*
    * Hurd allows to open("/dev/fd/%u") to open the very same fd, but it's not
