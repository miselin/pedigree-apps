# Pedigree PKGBUILD Script
#
# What:       Netutils
# Is a:       Port
# Maintainer: Matthew Iselin

pkgname=netutils
pkgver=unknown
pkgrel=1
pkgdesc="Netutils: a suite of network utilities"
arch=('i686')
url=""
license=('GPL')
groups=()
depends=()
makedepends=()
provides=()
conflicts=()
replaces=()
backup=()
install=
source=($pkgname-$pkgver.tar.gz)
noextract=()
md5sums=() #generate with 'makepkg -g'

build() {
  cd "$srcdir/$pkgname-$pkgver"
  cp -R ./* $pkgdir/
}