prefix=/support/slang
exec_prefix=${prefix}
libdir=${prefix}/lib
includedir=${prefix}/include

Name: slang 
Description: S-Lang programming library and interpreter
URL: http://www.jedsoft.org/slang/
Version: 2.2.1
Libs: -L${libdir} -lslang
Libs.private:    -lm
Cflags: -I${includedir}
