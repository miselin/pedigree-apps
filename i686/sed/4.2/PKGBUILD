# Pedigree PKGBUILD Script
#
# What:       sed
# Is a:       Port
# Maintainer: Matthew Iselin

pkgname=sed
pkgver=4.2
pkgrel=1
pkgdesc="Text processor"
arch=('i686')
url=""
license=('GPL')
groups=()
depends=()
makedepends=()
provides=()
conflicts=()
replaces=()
backup=()
install=
source=($pkgname-$pkgver.tar.gz)
noextract=()
md5sums=() #generate with 'makepkg -g'

build() {
  cd "$srcdir/$pkgname-$pkgver"
  cp -R ./* $pkgdir/
}