# Pedigree PKGBUILD Script
#
# What:       Pedigree POSIX Subsystem
# Is a:       Native Application
# Maintainer: Matthew Iselin

pkgname=pedigree-posix
pkgver=0.1
pkgrel=1
pkgdesc="Pedigree POSIX Subsystem"
arch=('i686')
url="http://pedigree-project.org"
license=('New BSD')
groups=()
depends=('')
makedepends=()
provides=()
conflicts=()
replaces=()
backup=()
install=
source=($pkgname-$pkgver.tar.gz)
noextract=()
md5sums=() #generate with 'makepkg -g'

build() {
  cd "$srcdir/$pkgname-$pkgver"
  cp -R ./* $pkgdir/
}